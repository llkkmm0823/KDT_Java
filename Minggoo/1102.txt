ajax는 무엇의 약자인가요? Asynchronous Javascript And XML
ajax의 a는 무슨 의미인가요? x는 무엇인가요? 비동기, XML <키>값</키>
ajax에서 XML대신 사용할 수 있는 것은? JSON
JSON은 무엇의 약자인가요? JSON(JavaScript Object Notation)
ajax에서 반드시 필요한 키 2가지는? {key:value, key:value}, url, success
$.ajax() 괄호안에 들어가는 데이터 타입은?JSON, {}
jquery와 javascript와의 관계 javascript에서 자주 사용하고 중요한 코드들을 쉽게 쓸 수 있도록 라이브러리로 만들어놓은 것이 jquery $(“#id”) ⇒ document.getElementById(“id”);
OX
ajax는 http통신이다.O(header+body)
ajax에서 data키는 데이터를 받을 때 사용한다.X(브라우저에서 서버로 데이터를 보낼 때)
